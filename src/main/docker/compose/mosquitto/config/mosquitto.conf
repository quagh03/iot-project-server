# Mosquitto config — local development only.
# Real auth/ACLs are introduced in Phase 4 (topic ACLs) and hardened in Phase 10.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
