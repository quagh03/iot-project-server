# Minimal broker config for the Testcontainers-backed MQTT integration tests —
# no persistence directives needed since the container is ephemeral per test run.
log_dest stdout
listener 1883
allow_anonymous true
